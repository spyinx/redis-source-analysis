# 🐛 Bug Fix 分析 - c0c886a8

## 基本信息

| 项目 | 内容 |
|------|------|
| **提交** | [c0c886a8](https://github.com/redis/redis/commit/c0c886a8cff45d9c772e107a7be188539b86b3e0) |
| **作者** | Vitah Lin |
| **日期** | 2026-07-10T08:46:11Z |
| **类型** | BUGFIX |
| **统计** | +1 -0 (1 个文件) |

## 提交消息

```
Skip unready fds in select event backend (#15446)
```

## Issue

After `select()` returns, the select backend loops over all registered
fds to find the ones that are ready. It already skips fds that are not
registered, but it did not skip registered fds that were not ready in
the current `select()` result.

Those fds have a computed mask of `AE_NONE`. They do not trigger any
read/write callback, but adding them to `eventLoop->fired` still makes
`aeProcessEvents()` count them as processed events.

## Change

Skip fds whose computed mask is `AE_NONE` before adding them to
`eventLoop->fired`.

This makes the select backend only report fds that are actually ready.


### 关联 PR

- **PR**: [#15446](https://github.com/redis/redis/pull/15446) - Skip unready fds in select event backend
- **状态**: closed
- **作者**: vitahlin


## 问题概述

此修复针对 Redis 的 **select 事件后端**（event backend）中处理文件描述符（fd）就绪状态的逻辑。

### 背景

Redis 支持多种事件后端（epoll、kqueue、select 等）。在 `select` 后端实现中，当遍历 `select()` 返回的就绪文件描述符集合时，代码需要正确识别哪些 fd 是真正"就绪"的（即确实有读写事件需要处理）。

### 修复内容

修复确保在处理 `select` 事件循环时，**跳过那些未标记为就绪的文件描述符**。如果未正确跳过这些"未就绪"的 fd，可能导致：

- 对未就绪的 fd 进行不必要的读写尝试
- 潜在的逻辑错误或性能问题
- 在极端情况下可能引发不稳定行为

## 文件变更

- `src/ae_select.c` (modified, +1/-0)

## 代码变更分析


#### `src/ae_select.c`

```diff
@@ -74,6 +74,7 @@ static int aeApiPoll(aeEventLoop *eventLoop, struct timeval *tvp) {
                 mask |= AE_READABLE;
             if (fe->mask & AE_WRITABLE && FD_ISSET(j,&state->_wfds))
                 mask |= AE_WRITABLE;
+            if (mask == AE_NONE) continue;
             eventLoop->fired[numevents].fd = j;
             eventLoop->fired[numevents].mask = mask;
             numevents++;
```


## 影响评估

| 维度 | 评估 |
|------|------|
| **影响范围** | select 事件后端用户 |
| **严重程度** | 中等 |
| **是否涉及数据安全** | 否 |
| **是否涉及性能** | 是（避免不必要的 I/O 操作） |

## 修复验证

### 验证步骤

1. 在仅使用 select 后端的平台上编译 Redis（某些平台默认不使用 epoll）
2. 运行高并发连接测试
3. 确认无异常 I/O 行为或错误日志

```bash
# 检查当前使用的事件后端
redis-cli INFO server | grep io_thread

# 运行基础 I/O 测试
redis-benchmark -t set,get -n 100000 -c 100
```

---

*文档自动生成于 2026-07-11*
*分析引擎: analyze_commit.py*
